package books.head_first_design_patterns._01_intro.SimUDuck.redesigned;

/***
 * QuackBehavior is the interface that just includes a quack() method
 * that needs to be implemented.
 */
public interface QuackBehavior {
    void quack();
}
